{
  "name": "json-surgeon",
  "version": "0.2.2",
  "description": "Perform conversions of all sorts on a stream of JSON objects (valid or not).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "PODER",
  "license": "ISC",
  "dependencies": {
    "command-line-args": "^5.1.1",
    "dirty-json": "^0.9.2",
    "event-stream": "^4.0.1",
    "JSONStream": "^1.3.5"
  }
}
